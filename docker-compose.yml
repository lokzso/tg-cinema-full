services:
  web:
    build: .
    env_file: .env
    ports: ["8000:8000"]
    volumes: ["./:/app"]
  bot:
    build: .
    env_file: .env
    command: python run_bot.py
    depends_on: [web]
    volumes: ["./:/app"]
